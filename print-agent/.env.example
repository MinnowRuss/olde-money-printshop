# Supabase (service role — full access for Realtime + Storage)
SUPABASE_URL=https://jfsshvumndymnekydnef.supabase.co
SUPABASE_SERVICE_ROLE_KEY=

# Shared secret matching PRINT_AGENT_SECRET on the Next.js app
PRINT_AGENT_SECRET=

# Next.js app base URL (no trailing slash)
APP_BASE_URL=https://printing.oldemoney.com

# CUPS printer name (from `lpstat -p`). Leave empty for system default.
CUPS_PRINTER=

# Poll intervals (ms)
CUPS_POLL_INTERVAL_MS=5000
FALLBACK_POLL_INTERVAL_MS=30000
